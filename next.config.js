/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: [
            'nl.mg.s.inty.io'
        ],
    }
};

module.exports = nextConfig;
